import Card from '../ui/Card';

export default function TemplateManager() {
  return <Card className="text-sm text-slate-600">Template manager legacy sẽ dùng lại endpoint `/automation/templates` ở bước parity chi tiết.</Card>;
}
