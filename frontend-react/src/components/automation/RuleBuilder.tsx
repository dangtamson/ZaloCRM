import Card from '../ui/Card';

export default function RuleBuilder() {
  return <Card className="text-sm text-slate-600">Rule builder legacy sẽ được nối mutation sau khi các route Bot-Auto ổn định.</Card>;
}
