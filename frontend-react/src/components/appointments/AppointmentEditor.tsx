import Card from '../ui/Card';

export default function AppointmentEditor() {
  return <Card className="text-sm text-slate-600">Tạo và chỉnh sửa lịch hẹn sẽ được nối mutation trong bước parity chi tiết.</Card>;
}
