import Card from '../ui/Card';

export default function DuplicateReviewDialog() {
  return (
    <Card className="text-sm text-slate-600">
      Kiểm tra trùng lặp sẽ dùng endpoint intelligence của CRM trong bước parity tiếp theo.
    </Card>
  );
}
